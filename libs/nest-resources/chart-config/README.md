# nest-resources-chart-config

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test nest-resources-chart-config` to execute the unit tests via [Jest](https://jestjs.io).
